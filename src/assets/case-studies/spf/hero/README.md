# hero

Resolved (Sprint 18.2) — no file needed in this folder.

The hero visual is `product/Desktop - 54.png` (the citizen-portal authentication screen), referenced directly from `pages/case-studies/SPF/Hero.tsx` rather than through this folder's checklist system. A single deliberate hero choice doesn't need a lookup mechanism — it needed a judgment call, which has been made.

If a better hero candidate turns up later, swap the import in `Hero.tsx`; this folder can stay empty.
